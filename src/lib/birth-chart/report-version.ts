/**
 * Persistence/pipeline version for the human Birth Chart Report.
 *
 * Contract v3 remains stable. Version 7 invalidates reports generated before
 * the deterministic `peregrine`/`unaspected` distinction was corrected and
 * before reports became eligible as bounded Oracle interpretation context.
 */
export const BIRTH_CHART_REPORT_VERSION = 7;

